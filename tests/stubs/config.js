// Stub fuer js/config.js. Die echte Datei ist gitignored (Secrets) und
// initialisiert Firebase, beides hat in Tests nichts verloren.
export const db = {};
export const auth = { currentUser: null };
