# @capacitor-firebase/authentication wants firebase ^12 as a peer, but only for its
# web implementation, which we never load. The firebase devDependency stays on 10.x
# to match vendor/firebase/10.12.0 in the tests.
legacy-peer-deps=true
