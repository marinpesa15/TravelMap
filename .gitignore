# Secret config files — copy from *.example.js and fill in real values
js/config.js
js/constants.js

# OS
.DS_Store

node_modules/
.firebase/hosting..cache
firestore-debug.log

# Firestore emulator logs
*-debug.log

# Staged web build for Capacitor (npm run build:web)
dist/

# Firebase iOS config, wie js/config.js nicht in git (Kopie in .local/)
ios/App/App/GoogleService-Info.plist
.cache/

# Kopien der gitignorten Geheimnisse (config.js, GoogleService-Info.plist).
# Liegen bewusst im Projekt, aber weder in git noch auf dem Hosting: der
# Punkt-Ordner faellt unter die ignore-Regel `**/.*` in firebase.json.
.local/
